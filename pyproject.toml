[project]
name = "github-ruleset-enforcement"
version = "0.1.0"
description = "Enforces a templated ruleset on all active non-fork GitHub repositories."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pyyaml==6.0.3",
    "requests==2.32.5",
]
